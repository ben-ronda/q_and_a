import { postPopularity } from 'q-and-a/helpers/post-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | post popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postPopularity([42]);
  assert.ok(result);
});
